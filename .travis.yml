language: python

python:
  - "3.6"

env:
  - DJANGO=2.0

install:
  - pip install "Django>=$DJANGO,<${DJANGO}.99"

script:
- "`which django-admin.py` test django_otp_cache --settings=sms_engine.test_settings --pythonpath=."
